<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.diet.mapper.UserMapper">

    <resultMap id="AppUserMap" type="com.diet.model.AppUser">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="displayName" column="display_name"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="AppUserMap">
        SELECT id, username, password_hash, display_name, enabled, created_at, updated_at
        FROM diet_user
        WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="AppUserMap">
        SELECT id, username, password_hash, display_name, enabled, created_at, updated_at
        FROM diet_user
        WHERE username = #{username}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.diet.model.AppUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO diet_user (username, password_hash, display_name, enabled, created_at, updated_at)
        VALUES (#{username}, #{passwordHash}, #{displayName}, #{enabled}, NOW(), NOW())
    </insert>
</mapper>
